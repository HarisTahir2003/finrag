# The package and its dependencies are declared in pyproject.toml. This file
# exists so `pip install -r requirements.txt` still works; it installs finrag in
# editable mode with the extras needed to run the notebook and the app.
#
# Extras, if you want a narrower install:
#   .[local]     sentence-transformers embeddings - no API key, used by CI
#   .[google]    Gemini + text-embedding-004 - needs GOOGLE_API_KEY
#   .[semantic]  structure-aware chunking via unstructured
#   .[app]       Streamlit front end
#   .[dev]       pytest + ruff

-e .[local,google,semantic,app,dev]
